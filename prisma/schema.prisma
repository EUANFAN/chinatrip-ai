generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Phase 1 keeps the schema as the planned data contract.
// Migrations are intentionally deferred until feature implementation begins.
